# AndroidX (required — all dependencies are AndroidX)
android.useAndroidX=true

# Use non-transitive R classes (recommended, reduces build conflicts)
android.nonTransitiveRClass=true

# Kotlin code style
kotlin.code.style=official

# Gradle JVM args
org.gradle.jvmargs=-Xmx4g -XX:+UseParallelGC
